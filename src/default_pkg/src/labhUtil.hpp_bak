#ifndef labhUtil_hpp
#define labhUtil_hpp

#include <string>

class labhUtil
{
	public:
		labhUtil(int ident);
		std::string getUSBloc();

	private:
		int _ident;
		int _port;
		int _baud;

};

#endif
